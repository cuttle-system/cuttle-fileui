#include "generator_ui.hpp"

using namespace cuttle;

namespace fs = boost::filesystem;

void fileui::get_generator_from_module(compile_state &state, const fs::path &module_path,
                                       generator_config_t &generator_config) {

}